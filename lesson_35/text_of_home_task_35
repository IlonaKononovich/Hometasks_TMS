Домашнее задание 35:
1. Загрузить файлы orders_data.xlsx и deliveries_data.xlsx в наш DWH в сырой слой (raw) в том же виде, как они находится в xlsx (as is).
2. Необходимо рассчитать витрину в data_mart на основе сырых данных по заказам пользователей.
Необходимо с помощью airflow взять данные из одной таблицы и второй, далее сджойнить и посчитать статистику либо в SQL либо в Airflow с помощью pandas и полученные результаты записать в таблицу в схеме data_mart.
Формат данных в витрине:
Имя пользователя, его карта и общая сумма заказа